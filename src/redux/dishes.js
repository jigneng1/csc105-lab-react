import { DISHES } from '../shared/dishes';

export const Dishes = (state = DISHES, action) => { //สร้าง reducer have 2 parameter state,action
    switch (action.type) {
        default:
          return state;
      }
};